# chat

- babel 실행 
```
// src폴더 아래의 .js, .jsx파일이 .tmp폴더 아래에 .js파일로 출력된다.
./node_modules/.bin/babel --out-dir .tmp src

// --watch 옵션을 사용하면 파일 변경을 감지해서 자동으로 변환해줍니다.
./node_modules/.bin/babel --watch --out-dir .tmp src
```

